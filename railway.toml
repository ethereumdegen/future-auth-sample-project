[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/app/migrate && /app/app"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
